# AG-001 acceptance evidence

This evidence file maps the local ticket without altering the preserved backlog or external
Linear state. The orchestrator owns final ticket status.

| Acceptance criterion | Evidence |
|---|---|
| Product terminology, lifecycle states, whole-CSV access, and deferred scope are approved. | Normative [`product-contract.md`](../product-contract.md), dated and change-controlled. |
| Selected stack and supported runtimes are recorded. | [`architecture.md`](../architecture.md) stack table and [`ADR 0001`](../adr/0001-foundation-stack.md); `.python-version`, `pyproject.toml`, and `uv.lock`. |
| Portal, content, identity, data, and storage boundaries are diagrammed, including local hostnames. | Mermaid boundary diagram, topology table, and boundary rules in [`architecture.md`](../architecture.md). |
| Uploaded HTML is prohibited from portal origin/DOM. | Normative product/security invariant; separate settings and empty content URLconf; portal host rejection and CSP smoke tests. |
| A new developer can install, start, and run checks from documented commands. | Root [`README.md`](../../README.md), `.env.example`, safe bootstrap, package-managed Oracle profile, and canonical `scripts/check.py`. |
| CI runs install, lint/static analysis, tests, and build checks. | `.github/workflows/ci.yml` locked install plus canonical gate; actions are immutable-SHA pinned and run on an Oracle-capable self-hosted runner. |
| Missing required configuration fails clearly and no secrets are committed. | [`configuration.md`](../configuration.md), typed `RuntimeConfig`, rejected placeholders, ignored `.env`, redaction tests, and generated local secrets. |

## Required verification

Run from a clean, bootstrapped development environment with the local Oracle profile reachable:

```powershell
uv sync --locked --all-groups
uv run --locked python scripts/bootstrap_env.py
$env:AGORA_ENVIRONMENT = "test"
$env:AGORA_TEST_DATABASE_RESET_ALLOWED = "true"
uv run --locked python scripts/check.py
```

The quality gate checks the dependency lock, formatting, lint, strict types, bytecode compilation,
both Django service compositions, static asset collection, migration drift/application,
Oracle-backed tests with branch coverage, and package build. CI runs the same gate from a
locked clean install. Set the two test values only after verifying that the selected Oracle
profile resolves to a dedicated disposable validation schema.
